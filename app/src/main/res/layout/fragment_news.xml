<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/blue_light"
    tools:context=".bottom_nav.FragmentNews">

    <!-- Заголовок "Статьи" -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Статьи"
        android:gravity="center"
        android:textSize="40sp"
        android:textColor="@color/black1"/>

    <!-- Кнопка добавления статьи (изначально скрыта) -->
    <Button
        android:id="@+id/add_article_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:text="Добавить статью"
        android:visibility="gone"/>

    <!-- RecyclerView для отображения статей -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_articles"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="80dp"/>
</FrameLayout>
